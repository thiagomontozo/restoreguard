import{expect,test}from'@playwright/test';
test('login to demo recovery overview and open asset',{tag:'@smoke'},async({page})=>{await page.goto('/login');await page.getByLabel('Email').fill(process.env.E2E_EMAIL??'admin@restoreguard.local');await page.getByLabel('Password').fill(process.env.E2E_PASSWORD??'restoreguard-demo-password');await page.getByRole('button',{name:'Sign in'}).click();await expect(page.getByRole('heading',{name:'Recovery Overview'})).toBeVisible();await page.getByRole('link',{name:'Protected Assets'}).click();await page.getByRole('link',{name:'Demo PostgreSQL ERP'}).click();await expect(page.getByRole('heading',{name:'Demo PostgreSQL ERP'})).toBeVisible();await expect(page.getByText('Recovery readiness')).toBeVisible()});
