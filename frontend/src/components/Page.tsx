import type{ReactNode}from'react';export function Page({title,description,action,children}:{title:string;description:string;action?:ReactNode;children:ReactNode}){return <><div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="eyebrow">RestoreGuard</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-ink">{title}</h1><p className="mt-2 max-w-3xl text-sm text-slate-600">{description}</p></div>{action}</div>{children}</>}
export function EmptyState({title,body,action}:{title:string;body:string;action?:ReactNode}){return <div className="card flex min-h-56 flex-col items-center justify-center text-center"><ShieldCheck className="mb-3 text-slate-400" aria-hidden/><h2 className="font-bold">{title}</h2><p className="mt-1 max-w-lg text-sm text-slate-600">{body}</p>{action&&<div className="mt-5">{action}</div>}</div>};import{ShieldCheck}from'lucide-react';
