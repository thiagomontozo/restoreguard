import{useState,type FormEvent}from'react';import{Navigate,useLocation,useNavigate}from'react-router-dom';import{ArchiveRestore}from'lucide-react';import{useAuth}from'../auth-context';
export function Login(){const{user,login}=useAuth();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);const navigate=useNavigate();const location=useLocation();if(user)return <Navigate to="/" replace/>;async function submit(event:FormEvent){event.preventDefault();setBusy(true);setError('');try{await login(email,password);navigate((location.state as{from?:string}|null)?.from??'/',{replace:true})}catch{setError('Unable to sign in. Check your email and password.')}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-canvas p-4"><section className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-8 shadow-lg" aria-labelledby="login-title"><div className="mb-8 flex items-center gap-3 text-navy"><span className="grid h-11 w-11 place-items-center rounded-xl bg-navy text-white"><ArchiveRestore/></span><div><p className="font-bold">RestoreGuard</p><p className="text-xs text-slate-500">Recovery Assurance Platform</p></div></div><h1 id="login-title" className="text-2xl font-bold">Sign in</h1><p className="mt-2 text-sm text-slate-600">Access controlled recovery evidence and drills.</p>{error&&<div role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-danger">{error}</div>}<form className="mt-6 space-y-4" onSubmit={submit}><label className="block text-sm font-semibold">Email<input className="mt-1.5 w-full rounded-lg border border-slate-300 px-3 py-2.5 font-normal" type="email" autoComplete="username" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block text-sm font-semibold">Password<input className="mt-1.5 w-full rounded-lg border border-slate-300 px-3 py-2.5 font-normal" type="password" autoComplete="current-password" required minLength={12} value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button-primary w-full" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><p className="mt-6 text-xs leading-relaxed text-slate-500">A successful backup job is not the same as a verified recovery.</p></section></main>}
